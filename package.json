{
  "name": "permitio-on-hono",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.10.4",
    "permitio": "^2.7.5"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "wrangler": "^4.4.0"
  }
}
